#pragma once

#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <windows.h>

namespace utils {
    void _throw(
        const std::string& title,
        const std::string& msg);
}